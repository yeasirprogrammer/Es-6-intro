const diffirent = (x, y)=> x-y 
const mulfiplay =( first, second, third) => first* second* third; 

// single paramater or one parameter 
const getAge = (person) => person.age;
const student = { name: 'anata ', age:45}
const age = getAge(student)
console.log(age);
const getThird = numbers => numbers[2];
const third= getThird([5,9,88,2,13])
const doubleIt = num => num*2; 

// no paramater 
const getPI =() => Math.PI
console.log(getPI);

// large arrow function
const doMath = (x,y,z) => {
    const sum = x + y + z;
    const multi = x* 

}